import { makeNewEvent } from './scripts';

export const DEFAULT_FontSize = 'base';

export const DEFAULT_PaddingTop = '56';

export const DEFAULT_Data = [
	{
		topic: '',
		events: [makeNewEvent()],
	},
];
